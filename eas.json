{
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "env": {
        "GOOGLE_SERVICES_JSON": "${secrets.GOOGLE_SERVICES_JSON}"
      },
      "android": {
        "buildType": "apk"
      }
    },
    "preview": {
      "distribution": "internal",
      "env": {
        "GOOGLE_SERVICES_JSON": "${secrets.GOOGLE_SERVICES_JSON}"
      },
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "env": {
        "GOOGLE_SERVICES_JSON": "${secrets.GOOGLE_SERVICES_JSON}"
      },
      "android": {
        "buildType": "apk"
      }
    }
  }
}
